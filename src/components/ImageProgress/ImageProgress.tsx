import { CircleSnail } from "react-native-progress";

/**
 * Adds a loading bar for images as they are downloaded
 */
const ImageProgress = (): JSX.Element => <CircleSnail />;

export default ImageProgress;
